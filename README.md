# LOCATE-paper-custom-scripts

This repository contains custom scripts used in the LOCATE paper, covering the entire pipeline from simulation data generation to tool evaluation.

- For details on the pipeline and scripts used in **simulation data generation, model training, and related processes**, see the [simulation](simulation/) directory.
- For details on the pipeline and scripts used in **tool evaluation and benchmarking**, see the [benchmark](benchmark/).
